version: '3'
services:
  todo-app-db:
    image: postgres:lastest
    container_name: todo-app-db
    restart: always
    environment:
      - 'POSTGRES_DB=${POSTGRES_DB}'
      - 'POSTGRES_USER=${POSTGRES_USER}'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
      - 'PGDATA=/pgdata'
    volumes:
      - ./data:/pgdata
    # ports:
    #   - 5555:5555
    networks:
      - mschool-backend-net
  todo-app-pgadmin:
    image: dpage/pgadmin4:lastest
    container_name: mschool-pgadmin
    restart: always
    environment:
      - 'PGADMIN_DEFAULT_EMAIL=${PGADMIN_EMAIL}'
      - 'PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PASSWORD}'
    ports:
      - '127.0.0.1:${PGADMIN_PORT}:80'
    volumes:
      - ./pgadmin-data:/var/lib/pgadmin
    networks:
      - mschool-backend-net
      - mschool-frontend-net
    depends_on:
      - mschool-db
  todo-app-web:
    image: nginx:1.25-alpine-slim
    container_name: todo-app-web
    restart: always
    volumes:
      - ./nginx:/etc/nginx/conf.d
    ports:
      -'${PUBLIC_PORT}:80'
    networks:
      - mschool-frontend-net
    depends_on:
      - todo-app-db
      - todo-app-pgadmin